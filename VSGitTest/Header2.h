#pragma once

#ifndef HEADER2_H
#define HEADER2_H

template <typename T>
inline T mul(T a, T b)
{
	return a * b;
}

template <typename T>
inline T div(T a, T b)
{
	return a / b;
}

#endif // !HEADER2_H