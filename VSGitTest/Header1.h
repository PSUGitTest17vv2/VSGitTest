#pragma once

#ifndef HEADER1_H
#define HEADER1_H

template <typename T>
inline T sum(T a, T b)
{
	return a + b;
}

template <typename T>
inline T sub(T a, T b)
{
	return a - b;
}

#endif // !HEADER1_H